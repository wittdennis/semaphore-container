branch_whitelist = []
disable_bump_commit = true
disable_changelog = true
from_latest_tag = false
generate_mono_repository_global_tag = true
ignore_merge_commits = false
post_bump_hooks = [
  "git push",
  "git push origin {{version}}",
]
post_package_bump_hooks = []
pre_bump_hooks = []
pre_package_bump_hooks = []
skip_ci = "[skip ci]"
skip_untracked = false

[git_hooks]

[commit_types]

[changelog]
authors = []
owner = "wittdennis"
path = "CHANGELOG.md"
remote = "github.com"
repository = "semaphore-container"
template = "remote"

[bump_profiles]

[monorepo.packages]
