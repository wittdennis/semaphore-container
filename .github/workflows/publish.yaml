name: publish-image

on:
  release:
    types: published

jobs:
  publish:
    uses: wittdennis/pipelines/.github/workflows/container-image-publish.yaml@2.0.1
    with:
      image-name: semaphore
    secrets:
      registry-password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}
