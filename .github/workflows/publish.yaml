name: publish-image

on:
  release:
    types: published

jobs:
  publish:
    uses: wittdennis/pipelines/.github/workflows/container-image-publish.yaml@29f41b6d8146f7189dce232aa5a23ee635f7cbe0 # 2.0.2
    with:
      image-name: semaphore
    secrets:
      registry-password: ${{ secrets.DOCKER_REGISTRY_TOKEN }}
